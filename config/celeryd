# Names of nodes to start
#   most people will only start one node:
CELERYD_NODES="k_spike1 k_spike2"
#   but you can also start multiple and configure settings
#   for each in CELERYD_OPTS
#CELERYD_NODES="worker1 worker2 worker3"
#   alternatively, you can specify the number of nodes to start:
#CELERYD_NODES=3

# Absolute or relative path to the 'celery' command:
#CELERY_BIN="celery"
CELERY_BIN="/home/kerickso/dev/virtual_envs/celery-spike/bin/celery"

# App instance to use
# comment out this line if you don't use an app
CELERY_APP="magic_engine.magical_functions"
# or fully qualified:
#CELERY_APP="proj.tasks:app"

# Where to chdir at start.
CELERYD_CHDIR="/home/kerickso/dev/celery-flask-aws-docker-example"

# Extra command-line arguments to the worker
CELERYD_OPTS="-Q kerickson_spike -n %h"
# Configure node-specific settings by appending node name to arguments:
#CELERYD_OPTS="--time-limit=300 -c 8 -c:worker2 4 -c:worker3 2 -Ofair:worker1"

# Set logging level to DEBUG
CELERYD_LOG_LEVEL="INFO"

# %n will be replaced with the first part of the nodename.
#CELERYD_LOG_FILE="/home/kerickso/notes/celery/%n%I.log"
#CELERYD_PID_FILE="/home/kerickos/notes/celery/%n.pid"

# Workers should run as an unprivileged user.
#   You need to create this user manually (or you can choose
#   a user/group combination that already exists (e.g., nobody).
#CELERYD_USER="kerickso"
#CELERYD_GROUP="geops"

# If enabled pid and log directories will be created if missing,
# and owned by the userid/group configured.
#CELERY_CREATE_DIRS=1
